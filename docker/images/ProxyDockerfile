# Create a proxy image from itzg's existent one
FROM itzg/bungeecord:latest

# Override default envs
ENV TYPE="WATERFALL" ENABLE_RCON="FALSE" HEALTH_USE_PROXY="TRUE" INIT_MEMORY="512M" MAX_MEMORY="2G"

# Disable healthcheck
HEALTHCHECK NONE